
import '../controller/linker/linker.dart';

class NavigationScreenTwo extends StatelessWidget {
  const NavigationScreenTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: MyText(txt: 'Screen Two',)),
    );
  }
}
